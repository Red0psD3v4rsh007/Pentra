"""Experimental / dormant engine modules.

These modules are **not imported at runtime** by any active service.
They represent future capability prototypes, research code, or ideas that
have not yet been integrated into the scan pipeline.

Classification: ``experimental``

To promote a module back to active status, move it to the parent
``engine/`` directory and wire it into the runtime import graph.
"""
