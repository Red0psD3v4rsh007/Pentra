# Raw Acquisition Manifests

These files define Phase 1 acquisition intent for each approved source.

Each manifest states:

- which source is being used
- why it is authoritative
- what Pentra should extract
- which normalized outputs should be produced

The manifest itself is not the learned fact set.
