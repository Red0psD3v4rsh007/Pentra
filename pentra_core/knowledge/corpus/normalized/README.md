# Normalized Knowledge Documents

These files are the Phase 1 structured outputs that Pentra can consume
directly.

Each document:

- is tied to one approved source or runtime-truth input
- carries citations for every normalized fact
- avoids long copied source text
- can be validated against the Phase 0 provenance contract
